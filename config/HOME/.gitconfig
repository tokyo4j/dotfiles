[user]
	name = tokyo4j
	email = hrak1529@gmail.com

[alias]
  tree = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) %C(white)%<(50,trunc)%s%C(reset) %C(dim white)%an%C(reset)%C(auto)%d%C(reset)' --all
  raw-log = log --format=%B -n 1
